import {Item} from "../Item";
import {CurrentInputs} from "../../controls/Inputs";


export class Grass extends Item {
    update(inputs?: CurrentInputs) {
    }

    draw(ctx: CanvasRenderingContext2D) {
    }

}